/**
 * An edge of a graph.
 */
public class Edge<E> {
    private E data;

    /**
     * Returns the data associated with the edge.
     * 
     * @return the data associated with the edge
     */
    public E getData() {
        return data;
    }
}